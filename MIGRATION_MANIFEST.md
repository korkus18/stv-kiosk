# Migration Manifest — Kiosk-C → STV Kiosk (production)

Generated by tracing all local imports from the entry point
`app/kiosk-C/page.tsx` in the source repo at
`/Users/macbook/Web Workspace/STV-group/STV-demo/scorpio`.

The dependency tree below is exactly what migrates — no demo scaffolding,
no A/B variants, nothing more.

---

## ⚠️ SHARED DEPENDENCY RESOLUTION

The kiosk-C tree pulls a **type-only** import from a file that is otherwise
A/B-variant code:

| Importer (kiosk-C tree)                 | Imports        | From                                 |
| --------------------------------------- | -------------- | ------------------------------------ |
| `components/kiosk/LightTacticalCanvas.tsx` | `type AnchorState` | `./TacticalChip` (A/B dark-theme chip) |
| `components/kiosk/TacticalChipLight.tsx`   | `type AnchorState` | `./TacticalChip` (A/B dark-theme chip) |
| `components/kiosk/KioskCLandscape.tsx`     | `type AnchorState` | `./TacticalChip` (A/B dark-theme chip) |
| `components/kiosk/KioskCPortrait.tsx`      | `type AnchorState` | `./TacticalChip` (A/B dark-theme chip) |

**Resolution:** `TacticalChip.tsx` is NOT copied. The 8-line `AnchorState`
type is moved into the renamed `HudChip.tsx` (formerly `TacticalChipLight.tsx`)
and re-imported from there by `KioskCanvas.tsx` and the two kiosk shells.
No A/B component code is brought along.

No other contamination found.

---

## DEPENDENCY TREE (files to copy)

Source paths are relative to `STV-demo/scorpio/`.
Target paths are relative to `stv-kiosk/`.

### Entry / routing
| Source                             | Target            | Notes                                |
| ---------------------------------- | ----------------- | ------------------------------------ |
| `app/kiosk-C/page.tsx`             | `app/page.tsx`    | Becomes the root route (renamed)     |
| `app/kiosk-C/layout.tsx`           | —                 | Drop (one-line wrapper, no value)    |
| `app/layout.tsx`                   | `app/layout.tsx`  | **Rewrite**: drop SectionSwitcher, fix metadata for production |
| `app/globals.css`                  | `app/globals.css` | Keep (dev-indicator rule retained)   |

### Components — kiosk
| Source                                              | Target                                          |
| --------------------------------------------------- | ----------------------------------------------- |
| `components/kiosk/KioskCLandscape.tsx`              | `components/kiosk/KioskLandscape.tsx`           |
| `components/kiosk/KioskCPortrait.tsx`               | `components/kiosk/KioskPortrait.tsx`            |
| `components/kiosk/CategoryFilterC.tsx`              | `components/kiosk/CategoryFilter.tsx`           |
| `components/kiosk/InventorySidebarC.tsx`            | `components/kiosk/InventorySidebar.tsx`         |
| `components/kiosk/InventoryOverlayC.tsx`            | `components/kiosk/InventoryOverlay.tsx`         |
| `components/kiosk/TacticalChipLight.tsx`            | `components/kiosk/HudChip.tsx`                  |
| `components/kiosk/LightTacticalCanvas.tsx`          | `components/kiosk/KioskCanvas.tsx`              |
| `components/kiosk/EmptyModelPlaceholder.tsx`        | `components/kiosk/EmptyModelPlaceholder.tsx`    |
| `components/kiosk/tokensC.ts`                       | `components/kiosk/tokens.ts`                    |
| `components/kiosk/utilsC.ts`                        | `components/kiosk/utils.ts`                     |

### Components — shared (icons + ui)
| Source                                          | Target                                       |
| ----------------------------------------------- | -------------------------------------------- |
| `components/icons/StvLion.tsx`                  | `components/icons/StvLion.tsx`               |
| `components/icons/category-icons.tsx`           | `components/icons/category-icons.tsx`        |
| `components/ui/STVLogo.tsx`                     | `components/ui/STVLogo.tsx`                  |

### Hooks / data
| Source                            | Target                          | Notes                       |
| --------------------------------- | ------------------------------- | --------------------------- |
| `hooks/useOrientation.ts`         | `hooks/useOrientation.ts`       |                             |
| `data/products.ts`                | `data/products.ts`              |                             |
| `data/products.json`              | `data/products.json`            | 23 products                 |
| `data/categories.ts` + `data/categories-c.ts` | `data/categories.ts` | **Merged** (see Phase 4)    |

### Config
| Source                       | Target                | Notes                                    |
| ---------------------------- | --------------------- | ---------------------------------------- |
| `package.json`               | `package.json`        | Renamed `scorpio` → `stv-kiosk`, deps trimmed (see below) |
| `tsconfig.json`              | `tsconfig.json`       | As-is                                    |
| `next.config.ts`             | `next.config.ts`      | Add `output: 'export'` + `images.unoptimized` |
| `postcss.config.mjs`         | `postcss.config.mjs`  | As-is                                    |
| `.gitignore`                 | `.gitignore`          | Add `reference/`, `.idea/`               |

### Package dependencies kept (used by tree)
- `next`, `react`, `react-dom` — framework
- `@react-three/fiber`, `@react-three/drei`, `three` — 3D
- `framer-motion` — animations (used by InventoryOverlay, InventorySidebar, HudChip)
- `lucide-react` — icons (used by EmptyModelPlaceholder for `Package`, categories.ts for category type icons)
- Tailwind/postcss config kept for `globals.css` compatibility (no Tailwind classes are actually used by the kiosk tree, but the postcss pipeline references it; safer to keep until verified)
- TypeScript devDeps + `@types/*`

### Package dependencies that can be pruned (NOT used by kiosk-C tree)
- `@gsap/react`, `gsap` — scroll animations on demo marketing pages
- `@studio-freight/lenis` — smooth-scroll provider for demo pages
- `qrcode`, `@types/qrcode` — only QRBlock used them (QRBlock not in tree)
- `@playwright/test` — used by demo audit scripts (not migrated)

Flagged for user to confirm pruning post-build (kept conservatively for now to
guarantee a clean install).

---

## PUBLIC ASSETS (to copy)

Referenced from `data/products.json` (`model3D` field) — three 3D models exist;
the remaining 20 products have `model3D: null` and use `EmptyModelPlaceholder`.

| Source                                                 | Target                                              |
| ------------------------------------------------------ | --------------------------------------------------- |
| `public/models/hayrick/` (entire dir, ~150+ files)     | `public/models/hayrick/`                            |
| `public/models/bangalore_torpedo_4x6ft/` (entire dir)  | `public/models/bangalore_torpedo_4x6ft/`            |
| `public/models/bangalore_torpedo_8x1m/` (entire dir)   | `public/models/bangalore_torpedo_8x1m/`             |
| `public/manifest.json`                                 | `public/manifest.json` (updated: `start_url: "/"`, drop missing icons) |

**Not copied:** `public/previews/`, `public/file.svg`, `public/globe.svg`,
`public/next.svg`, `public/vercel.svg`, `public/window.svg` — none referenced
by the kiosk-C tree.

**Not copied:** `STV-demo/stv-lion.svg` — `StvLion.tsx` is inline SVG; no
external SVG file is referenced.

---

## RENAME MAP (applied in Phase 4)

### Files (covered above by source→target table)

### Identifiers (and all import sites)
| Old                       | New                  |
| ------------------------- | -------------------- |
| `KioskCLandscape`         | `KioskLandscape`     |
| `KioskCPortrait`          | `KioskPortrait`      |
| `KioskCPage`              | `KioskPage`          |
| `KioskCSharedProps`       | `KioskSharedProps`   |
| `CategoryFilterC`         | `CategoryFilter`     |
| `InventorySidebarC`       | `InventorySidebar`   |
| `InventoryOverlayC`       | `InventoryOverlay`   |
| `TacticalChipLight`       | `HudChip`            |
| `LightTacticalCanvas`     | `KioskCanvas`        |
| `HudAnchorC`              | `HudAnchor`          |
| `C` (tokens namespace)    | `tokens`             |
| `CATEGORIES_C`            | `CATEGORIES`         |
| `CategoryMetaC`           | `CategoryMeta`       |
| `getCategoryC`            | `getCategory`        |

### CSS class / keyframe identifiers
| Old                          | New                       |
| ---------------------------- | ------------------------- |
| `inventory-pulse-dot-c`      | `inventory-pulse-dot`     |
| `inventoryPulseDotC` (kf)    | `inventoryPulseDot`       |
| `tacticalChipPulseLight` (kf)| `hudChipPulse`            |

### Routes / chrome
- `app/kiosk-C/` → deleted (entry moved to `app/page.tsx`)
- `<Link href="/">` `Hub` link in both layouts → removed entirely
- Title metadata "STV Kiosek — Návrh C · Operational Light" → "STV GROUP — Kiosk"

### categories.ts merge decision
`data/categories.ts` (Lucide icons + cs/en labels) is **merged** with
`data/categories-c.ts` (English label + iconKey for brand-faithful icons).
The brand-faithful icons in `components/icons/category-icons.tsx` are the
ones actually used by the kiosk UI. Result: a single `data/categories.ts`
exporting `CategoryId`, `CategoryMeta` (id + label + iconKey), `CATEGORIES`,
`getCategory`. Lucide imports drop out (no usage in the migrated tree).

---

## CONTAMINATION CHECK — RESULT

- ❌ NOT imported by kiosk-C tree (good): `TacticalKioskCanvas`,
  `PremiumLightKioskCanvas`, `MorphBulletCanvas`, `BackToHub`,
  `CategoryFilter` (non-C), `CategoryPlaceholder`, `InventoryItem`,
  `InventorySearch`, `InventorySidebar` (non-C), `MissionIdStrip`,
  `MissionStripC`, `ProductCard`, `QRBlock`, `ScanlineSweep`,
  `SpecGroupedTable`, `TacticalChip`, `TacticalGrid`, anything under
  `components/design-b/`, anything under `components/sections/`,
  `components/3d/*`, `components/SmoothScrollProvider`, `lib/animations`,
  `lib/kioskProducts`, `lib/products`, `lib/useMediaQuery`.

- ⚠️ TYPE-ONLY import: `AnchorState` from `TacticalChip` — see
  resolution at the top. Type is inlined into the renamed `HudChip.tsx`;
  `TacticalChip.tsx` itself is NOT copied.

Total component/data/hook source files copied: **15** (incl. merged categories).
Plus 4 config files + assets.
